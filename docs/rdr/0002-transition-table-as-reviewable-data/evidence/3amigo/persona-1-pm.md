# Persona 1 — Product Manager

- **PM-001** — The user outcome is "every legal edge lives in one reviewable artifact," but the locked contract does not name the concrete TOML field layout even though Research Findings say Resolve identified the root-key placement constraint. Trigger: `Key Discoveries` says the exact field layout was proven, while `Normative Contracts` only says "sparse TOML data."
- **PM-002** — The reviewable artifact goal depends on diagnostics leading reviewers back to authored rules, but the draft says "source span" without defining the user-visible locator. Trigger: `Normative Contracts` requires candidate rows retain "source rule id and source span."

