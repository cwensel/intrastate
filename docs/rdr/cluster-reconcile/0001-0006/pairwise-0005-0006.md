# Pairwise 0005-0006

Date: 2026-06-24

## Findings

| Type | Severity | A quote | B quote | Explanation |
| --- | --- | --- | --- | --- |
| gap | risks-impl | RDR 0005: "no new envelope fields or exit groups required" | RDR 0006: "extend `internal/cli/clierr.CLIError` with an optional typed `Findings` field" | This is not a direct conflict because RDR 0005's claim is scoped to resolver/accessor failures, but implementation could overread it as global CLI envelope guidance. RDR 0006 remains the exception for graph-lint structured findings. |
| no conflict | cosmetic | RDR 0005: "CLI may later expose lint, but this RDR does not own lint guarantees." | RDR 0006: "The authoritative user-facing surface is the root command `intrastate lint`." | No contradiction. RDR 0005 owns runtime `flow` verbs; RDR 0006 owns root lint authority. |

## Disposition

No SPEC-DEFECT. Carry the envelope-field note into implementation review; it
does not require demotion because RDR 0006 already owns the exception.

