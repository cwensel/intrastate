# Pairwise 0001-0002

Date: 2026-06-24

## Findings

| Type | Severity | A quote | B quote | Explanation |
| --- | --- | --- | --- | --- |
| no conflict | cosmetic | RDR 0001: "refuses zero or multiple matches unless the table contract explicitly models an escape edge" | RDR 0002: "If zero or multiple non-escape rows match, the resolver may return a modeled escape disposition only when exactly one escape row matches the same tag-set and its `escape` list contains the corresponding failure class" | RDR 0002 now defines how authored escape rows normalize and when the resolver may use them, so the earlier escape-edge gap is closed. |
| no conflict | cosmetic | RDR 0001: "parser bugs, IO failures, or programmer mistakes" | RDR 0002: "malformed TOML or schema-invalid rules are load/lint failures before the resolver sees a table" | RDR 0002 now keeps malformed source/model validation outside resolver typed refusals, preserving RDR 0001's kernel boundary. |

## Disposition

No SPEC-DEFECT. RDR 0002's current Final text carries the escape-row syntax,
normalization contract, exact-one selection rule, and validation/refusal
boundary needed by RDR 0001.
