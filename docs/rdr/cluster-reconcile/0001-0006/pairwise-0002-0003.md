# Pairwise 0002-0003

Date: 2026-06-24

## Findings

| Type | Severity | A quote | B quote | Explanation |
| --- | --- | --- | --- | --- |
| gap | risks-impl | RDR 0002: "a legal recognized-outcome alphabet" and "declare every tag it matches or writes, including each tag's provenance" | RDR 0003: "one source-state/recognized-outcome selection group" | The RDRs imply recognized outcomes are both a root alphabet and possibly declared recognized tags, but do not lock the invariant between those two representations. Implementers need to know whether every `outcomes` value corresponds to a declared recognized tag, whether `outcome` is reserved, and which surface drives `unmodeled_outcome`. |
| duplication | cosmetic | RDR 0002: "Guard predicates MUST be represented as positive `all` predicates and negative `unless` predicates." | RDR 0003: "Positive guard atoms MUST live in `all`; negative guard atoms MUST live in `unless`." | Benign duplication because RDR 0002 owns the TOML container and RDR 0003 owns predicate semantics. Future edits should keep this split explicit. |

## Disposition

No additional demotion beyond RDR 0002. The recognized-outcome/tag invariant
can be handled inside the RDR 0002 Stage 4 re-entry already opened by the
0001-0002 escape-edge defect.

