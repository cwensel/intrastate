# Pairwise 0002-0003

Date: 2026-06-24

## Findings

| Type | Severity | A quote | B quote | Explanation |
| --- | --- | --- | --- | --- |
| no conflict | cosmetic | RDR 0002: "Recognized outcome alphabet: the closed set of outcome tags that the recognizer may emit for the flow" and "The model MUST declare every tag it matches or writes, including each tag's provenance" | RDR 0003: "Predicate lint MUST distinguish owned, observed, and recognized tags" | RDR 0002 owns the recognized alphabet and tag declarations; RDR 0003 consumes recognized provenance for predicate/lint reasoning. No remaining ownership gap blocks implementation. |
| duplication | cosmetic | RDR 0002: "Guard predicates MUST be represented as positive `all` predicates and negative `unless` predicates." | RDR 0003: "Positive guard atoms MUST live in `all`; negative guard atoms MUST live in `unless`." | Benign duplication because RDR 0002 owns the TOML container and RDR 0003 owns predicate semantics. Future edits should keep this split explicit. |

## Disposition

No SPEC-DEFECT. RDR 0002 remains the container/source-schema authority; RDR
0003 remains the predicate semantics and finite-domain proof authority.
