# Pairwise 0004-0005

Date: 2026-06-24

## Findings

| Type | Severity | A quote | B quote | Explanation |
| --- | --- | --- | --- | --- |
| no conflict | cosmetic | RDR 0004: "A gate accessor MUST return allow, deny, or indeterminate." | RDR 0005: "flow read-state MUST invoke only declared read accessors" and "It MUST NOT invoke gate accessors or coerce gate allow, deny, or indeterminate results into tag values." | RDR 0005 now preserves the RDR 0004 capability split: read-state reads tags, while gate accessors surface only through next/resolve candidate facts or refusals. |
| round-trip | cosmetic | RDR 0004: "write -> read = expected owned-tag value identity + protected non-owned tag identity" | RDR 0005: "`set-state` followed by `read-state` must return the expected owned-tag values" | No contradiction. RDR 0005 correctly narrows the CLI-visible invariant and delegates protected non-owned tag verification to RDR 0004. |

## Disposition

No SPEC-DEFECT. RDR 0005's current Final text keeps read, gate, and write
accessor capabilities separated at the CLI boundary.
