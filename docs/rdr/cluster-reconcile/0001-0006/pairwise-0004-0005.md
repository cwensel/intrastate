# Pairwise 0004-0005

Date: 2026-06-24

## Findings

| Type | Severity | A quote | B quote | Explanation |
| --- | --- | --- | --- | --- |
| gap | risks-impl | RDR 0004: "A gate accessor MUST return allow, deny, or indeterminate." | RDR 0005: "flow read-state MUST invoke only declared read or gate accessors over caller-supplied `role=path` artifact bindings and return the read tag-set or a stable CLIError failure." | `read-state` may invoke gate accessors, but RDR 0004 gate accessors do not return tag values. RDR 0005 must either restrict `read-state` to read accessors or define how gate results appear in a read-state response. |
| round-trip | cosmetic | RDR 0004: "write -> read = expected owned-tag value identity + protected non-owned tag identity" | RDR 0005: "`set-state` followed by `read-state` must return the expected owned-tag values" | No contradiction. RDR 0005 correctly narrows the CLI-visible invariant and delegates protected non-owned tag verification to RDR 0004. |

## Disposition

SPEC-DEFECT against RDR 0005, the less foundational RDR. Demote RDR 0005 to
Draft and re-enter at Stage 4 with STAGE-SCOPED re-verification of A4.
